# Notes: cli-core

## Open Questions

- Should `doctor` and `check` also grow `--json` in the next iteration?

## Technical Debt

- `scripts/noc.py` is a large single-file CLI. Keep changes localized unless a split becomes necessary for real complexity.

## Future Ideas

- Add stable schema documentation for `work --json`.
