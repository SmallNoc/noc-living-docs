# Gemini Project Protocol

Use the NOC Living Docs protocol in `noc_docs/`.

See `templates/AGENTS.md` for the canonical managed block. Keep this file consistent with that protocol when using Gemini CLI.

